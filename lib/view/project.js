const $ = require('jquery');
const { DomView, template, find, from, Model, attribute, bind } = require('janus');
const { Project, Assembly, Stack } = require('../model/project');




/*module.exports = {
  ProjectView, AssemblyView, StackView,
  register(library) {
    library.register(Project, ProjectView);
    library.register(Assembly, AssemblyView);
    library.register(Stack, StackView);
  }
};*/

